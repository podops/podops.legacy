dispatch:
  - url: "*/a/v1/*"
    service: api

  - url: "*/_a/*"
    service: api

  - url: "*/_t/*"
    service: api

# FIXME change to api
  - url: "api.podops.dev/q/*"
    service: default

# FIXME remove this and then only deploy a 'default' service
  - url: "cdn.podops.dev/*"
    service: default

